# VNC для отладки браузеров в Docker

Этот документ описывает, как использовать VNC для визуальной отладки браузеров в Docker контейнерах.

## 🎯 Зачем это нужно?

Когда браузеры запускаются с `headless=False` внутри Docker контейнера, вы не можете видеть, что происходит. VNC позволяет подключиться к виртуальному дисплею контейнера и наблюдать за работой браузеров в реальном времени.

## 📋 Что включено

- **x11vnc** - VNC сервер для X11
- **noVNC** - веб-интерфейс для VNC (не требует установки VNC клиента)
- **fluxbox** - легковесный оконный менеджер

## 🚀 Быстрый старт

### 1. Browser Service (Завод)

```bash
cd browser_service
docker-compose -f docker-compose.vnc.yml up --build -d
```

**Доступ к VNC:**
- Веб-интерфейс (noVNC): http://localhost:6080
- VNC клиент (если установлен): `vnc://localhost:5900`

### 2. Monitor Service (Ферма)

```bash
cd monitor_service
docker-compose -f docker-compose.vnc.yml up --build -d
```

**Доступ к VNC:**
- Веб-интерфейс (noVNC): http://localhost:6081
- VNC клиент (если установлен): `vnc://localhost:5901`

## 📖 Использование

### Через веб-браузер (noVNC)

1. Откройте http://localhost:6080 (browser_service) или http://localhost:6081 (monitor_service)
2. Нажмите "Connect" - пароль не требуется (для отладки отключен)
3. Вы увидите виртуальный рабочий стол с браузерами

### Через VNC клиент

Если у вас установлен VNC клиент (например, RealVNC, TightVNC):

```bash
# Browser Service
vncviewer localhost:5900

# Monitor Service
vncviewer localhost:5901
```

## 🔍 Что можно увидеть

- **Browser Service**: Браузеры из пула, когда они открывают страницы и выполняют действия
- **Monitor Service**: Браузеры мониторов, когда они проверяют лиды и логинятся

## ⚙️ Настройка

### Разрешение экрана

По умолчанию: 1920x1080x24

Чтобы изменить, отредактируйте `start_with_vnc.sh`:
```bash
Xvfb :99 -screen 0 1920x1080x24 ...
# Измените на нужное разрешение, например: 1280x720x24
```

### Отключение VNC

Если VNC не нужен, используйте обычные docker-compose.yml файлы:

```bash
# Browser Service
docker-compose up -d

# Monitor Service  
docker-compose up -d
```

## 📝 Примечания

1. **Производительность**: VNC добавляет небольшую нагрузку. Для production используйте обычные версии без VNC.

2. **Безопасность**: VNC запущен без пароля (для отладки). **НЕ используйте в production!**

3. **Порты**: 
   - Browser Service: 5900 (VNC), 6080 (noVNC)
   - Monitor Service: 5901 (VNC), 6081 (noVNC)

4. **Одновременный запуск**: Можно запустить оба сервиса с VNC одновременно - порты разные.

## 🐛 Отладка

Если VNC не работает:

1. Проверьте логи:
   ```bash
   docker logs browser_service
   docker logs monitor_service
   ```

2. Проверьте, что порты открыты:
   ```bash
   netstat -an | grep -E "(5900|5901|6080|6081)"
   ```

3. Перезапустите контейнеры:
   ```bash
   docker-compose -f docker-compose.vnc.yml restart
   ```

## ✅ Готово!

Теперь вы можете видеть, что делают браузеры в реальном времени. Это очень полезно для отладки и понимания работы системы.

